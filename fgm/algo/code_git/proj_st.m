function y = proj_st(y)
%--------------------------------------------------------------------------
% projection onto the stiefel manifold 

[u, ~, v] = svd(y);
y = u*v';

%-------------------------------------------------------------------------

end